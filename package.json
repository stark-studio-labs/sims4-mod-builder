{
  "name": "sims4-mod-builder",
  "version": "0.1.0",
  "description": "Build Sims 4 mods visually. No XML required.",
  "private": true,
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.js"
    ]
  }
}
